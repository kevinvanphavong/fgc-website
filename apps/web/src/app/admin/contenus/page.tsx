import ComingSoon from '@/components/admin/ComingSoon';

export const metadata = { title: 'Contenus du site' };

export default function AdminContenusPage() {
  return (
    <ComingSoon
      title="Contenus du site"
      subtitle="Formules, tarifs, activités, horaires, happy hours, exceptions."
      pr="PR4 — feat/admin-contenus (remplace EasyAdmin)"
    />
  );
}
